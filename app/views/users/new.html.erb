<% provide(:title, 'Sign up') %>
<% provide(:heading_title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>

<section class="section">
  <div class="columns is-centered">
    <div class="column is-one-third">
      <%= render 'form' %>
    </div>
  </div>
</section>
