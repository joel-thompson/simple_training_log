<% provide(:title, 'Add Technique') %>
<% provide(:heading_title, 'Technique') %>
<% provide(:heading_subtitle, 'New') %>
<% provide(:button_text, 'Add') %>

<section class="section">

  <div class="columns is-centered">
    <div class="column is-one-third">
      <%= render 'form' %>
    </div>
  </div>

</section>
