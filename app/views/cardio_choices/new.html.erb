<% provide(:title, 'Cardio Choices') %>
<% provide(:heading_title, 'Cardio Choices') %>
<% provide(:heading_subtitle, 'New') %>
<% provide(:button_text, 'Save!') %>

<section class="section">

  <div class="columns is-centered">
    <div class="column is-one-third">
      <%= render 'form' %>
    </div>
  </div>

</section>
